# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_excuses --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: excuses

on:
  workflow_dispatch:
    inputs:
      DUMMY_NAME:
        description: "DUMMY_NAME"
        required: false
  schedule:
    - cron: '0 * * * *'

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Cache ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Run './build.cmd NewExcuse'
        run: ./build.cmd NewExcuse
        env:
          DUMMY_NAME: ${{ github.event.inputs.DUMMY_NAME }}
          NUKE_EXCUSES_GET_ENDPOINT: ${{ secrets.NUKE_EXCUSES_GET_ENDPOINT }}
          NUKE_EXCUSES_REGEX: ${{ secrets.NUKE_EXCUSES_REGEX }}
